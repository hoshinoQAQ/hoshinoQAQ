const canvas=document.getElementById('particles');const ctx=canvas.getContext('2d');let w,h,particles=[];function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight}addEventListener('resize',resize);resize();function make(){particles=Array.from({length:90},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*3+1,vx:(Math.random()-.5)*.35,vy:Math.random()*.45+.12,a:Math.random()*Math.PI*2}))}make();function draw(){ctx.clearRect(0,0,w,h);for(const p of particles){p.x+=p.vx;p.y+=p.vy;p.a+=.02;if(p.y>h+10){p.y=-10;p.x=Math.random()*w}ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.a);ctx.fillStyle='rgba(255,121,184,.55)';ctx.beginPath();ctx.moveTo(0,-p.r*2);ctx.lineTo(p.r,p.r);ctx.lineTo(-p.r,p.r);ctx.closePath();ctx.fill();ctx.restore()}requestAnimationFrame(draw)}draw();let audioCtx,playing=false,osc,gain,lfo;function ping(freq=720,duration=.12){if(!audioCtx)return;const o=audioCtx.createOscillator();const g=audioCtx.createGain();o.type='sine';o.frequency.value=freq;g.gain.setValueAtTime(.0001,audioCtx.currentTime);g.gain.exponentialRampToValueAtTime(.08,audioCtx.currentTime+.01);g.gain.exponentialRampToValueAtTime(.0001,audioCtx.currentTime+duration);o.connect(g);g.connect(audioCtx.destination);o.start();o.stop(audioCtx.currentTime+duration+.03)}document.getElementById('soundBtn').addEventListener('click',async()=>{if(!audioCtx)audioCtx=new AudioContext();if(audioCtx.state==='suspended')await audioCtx.resume();ping();if(!playing){osc=audioCtx.createOscillator();gain=audioCtx.createGain();lfo=audioCtx.createOscillator();const lfoGain=audioCtx.createGain();osc.type='triangle';osc.frequency.value=220;lfo.frequency.value=.18;lfoGain.gain.value=18;lfo.connect(lfoGain);lfoGain.connect(osc.frequency);gain.gain.value=.025;osc.connect(gain);gain.connect(audioCtx.destination);osc.start();lfo.start();playing=true;document.getElementById('soundBtn').textContent='音乐播放中 · 点击音效已开启'}else{ping(880)}});document.addEventListener('click',e=>{if(e.target.id!=='soundBtn'&&audioCtx)ping(520+Math.random()*260,.08)});